# Dashboards

Lakeview (AI/BI) dashboard definitions for the Retail Bank Fraud demo.

- **Retail_Bank_Fraud_Dashboard.lvdash.json** — Template for the main fraud analytics dashboard. Deploy with `deploy.sh`; the script injects your Unity Catalog and schema so datasets query the correct `gold_transactions` view.
